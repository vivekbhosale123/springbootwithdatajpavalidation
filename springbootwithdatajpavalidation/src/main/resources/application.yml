server:
  port: 2025

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jpavalidation
    username: postgres
    password: Aai@8530
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
